import { styled } from "styled-components";

export const StyledSelected = styled.section`
max-width: 80%;
margin: 0 auto;
display: flex;
flex-direction: column;


    span:first-child {
      text-align: center;
      color: var(--Color-Black);
      font-family: var(--Font-Secundary);
      font-size: 18px;
      font-weight: 500;
      text-transform: uppercase;
    }
`